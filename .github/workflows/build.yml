name: Build Aimlock IPA

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 15.4
        run: |
          sudo xcode-select -s /Applications/Xcode_15.4.app
          xcodebuild -version

      - name: Create Xcode project
        run: |
          mkdir -p ios/Aimlock.xcodeproj

          cat > ios/project.pbxproj <<'EOF'
          // placeholder
          EOF

      - name: Build
        run: |
          echo "Project generation needs to be configured"
